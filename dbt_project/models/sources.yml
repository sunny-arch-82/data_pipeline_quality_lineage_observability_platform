version: 2

sources:
  - name: raw
    description: "Raw source data"
    tables:
      - name: orders
        description: "Raw orders table"
        columns:
          - name: id
            tests:
              - not_null
              - unique
